apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: envoy-gateway
  namespace: flux-system
spec:
  targetNamespace: envoy-gateway-system
  interval: 10m
  chart:
    spec:
      chart: gateway-helm
      version: "1.7.x"
      sourceRef:
        kind: HelmRepository
        name: envoy-gateway
      interval: 10m
